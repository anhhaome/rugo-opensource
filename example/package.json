{
  "name": "example",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "mongo": "rugo --prepare && cd .rugo && sudo docker compose up",
    "dev": "rugo",
    "restore": "rugo --restore"
  },
  "devDependencies": {
    "@rugo-vn/open": "../"
  }
}
